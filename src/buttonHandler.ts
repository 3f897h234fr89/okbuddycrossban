import * as Discord from "discord.js";

export function handleButton(interaction: Discord.ButtonInteraction, client: Discord.Client) {
    const { guild, user, member, customId, channel } = interaction;

    
}
